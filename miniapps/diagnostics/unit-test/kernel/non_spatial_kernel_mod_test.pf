!-----------------------------------------------------------------------------
! (c) Crown copyright 2021 Met Office. All rights reserved.
! The file LICENCE, distributed with this code, contains details of the terms
! under which the code may be used.
!-----------------------------------------------------------------------------

!> test the non-spatial kernel
!>
!-------------------------------------------------------------------------------
module non_spatial_kernel_mod_test

    ! Global use statements go here

    use constants_mod, only : r_def, i_def

    use pFUnit_Mod

    implicit none

    private
    public :: non_spatial_kernel_test

    ! The test case type, containing procedures to setup, run
    ! and clean up after a test.
    ! It can also contain data.

    @TestCase
    type, extends(TestCase), public :: non_spatial_kernel_mod_test_type
        private
    contains
        procedure setUp
        procedure tearDown
        procedure non_spatial_kernel_test
    end type non_spatial_kernel_mod_test_type


contains

    !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
    !> The setUp subroutine is optional and is called prior to running the test.
    !> It is used to create and initialise types / data that are used in
    !> the tests
    subroutine setUp(this)

        implicit none

        class(non_spatial_kernel_mod_test_type), intent(inout) :: this

        ! This section should create and initialise anything needed for the test

    end subroutine setUp

    !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
    !> The tearDown subroutine is optional and called after the test.
    !> It is used to destroy any types / data previously created by setUp()
    subroutine tearDown(this)

        implicit none

        class(non_spatial_kernel_mod_test_type), intent(inout) :: this

        ! This section should destroy any previously created types / data

    end subroutine tearDown

    !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
    ! pfUnit will run each subroutine preceded by the @test line
    @test
    subroutine non_spatial_kernel_test(this)

        ! This section is where the code for a self-contained test should be put

        use non_spatial_kernel_mod, only : non_spatial_code

        implicit none

        class(non_spatial_kernel_mod_test_type), intent(inout) :: this
        integer(kind = i_def) :: dof = 2 ! can be changed but update dimensions below
        integer(kind = i_def) :: layers = 2
        integer(kind = i_def) :: ndata = 3
        integer(kind = i_def) :: i, j, k
        integer(kind = i_def) :: counter = 1
        integer(kind = i_def), dimension(2) :: field_dof_map ! dof
        real(kind = r_def) :: tol
        real(kind = r_def), dimension(12) :: field ! dof * layers * ndata

        field = 0.0_r_def

        ! Build up the dof map
        do i = 1, dof
            field_dof_map(i) = counter
            counter = counter + layers*ndata ! advance the counter ready for the next column
        end do

        ! Do it!
        call non_spatial_code(layers, & !set
                field, & !calculated
                ndata, & !set
                dof, & !set
                dof * layers * ndata, & !set
                field_dof_map) !calculated

        tol = 1.0e-14_r_def

        ! loop through all the dof on the base cell of the column

        do i = 1, dof
            do j = 1, ndata
                do k = 1, layers
                    ! check the field is equal to ndata index
                    @assertEqual(j, field(field_dof_map( i ) + (j-1)*layers + (k-1) ), tol)
                end do
            end do
        end do

    end subroutine non_spatial_kernel_test

end module non_spatial_kernel_mod_test
